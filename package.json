{
  "name": "n8n-owntracks-monorepo",
  "version": "1.0.0",
  "description": "Monorepo bridging OwnTracks & n8n - lightweight and easy to deploy",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "bun run --workspaces build",
    "dev": "bun run --workspaces dev",
    "lint": "bun run --workspaces lint",
    "clean": "bun run --workspaces clean && rm -rf node_modules bun_modules"
  },
  "keywords": [
    "owntracks",
    "n8n",
    "location",
    "tracking",
    "automation"
  ],
  "author": "",
  "license": "MIT",
  "engines": {
    "bun": ">=1.3.0"
  }
}
